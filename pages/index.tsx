import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
    return (
        <div>
            <Head>
                <title>Makje Shop</title>
                <meta name="description" content="Online Shop by Makje" />
                <link rel="icon" href="/favicon.ico" />
            </Head>

            <main>MAKJE SHOP</main>
        </div>
    );
};

export default Home;
